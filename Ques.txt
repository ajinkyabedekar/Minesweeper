1. ARRAYS:C supports multidimensional arrays. The simplest form of the multidimensional array is the two-dimensional array. You can pass to the function a pointer to an array by specifying the array's name without an index. C allows a function to return an array.


2. ARRAYS:C supports multidimensional arrays. The simplest form of the multidimensional array is the two-dimensional array. You can pass to the function a pointer to an array by specifying the array's name without an index. C allows a function to return an array.


3. Other User will not be able to user/write/read in leaderboard file if one user is writing it. If all users are reading file than no error will occur.


4. NOT APPLICABLE as user will disconnet and have to try after some time.


5. To compile server type

gcc -pthread server.c -o server

./server



To compile client type

gcc client.c minesweeper.c -o game

./game


